<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.geoweb.demo.mapper.UserMapper">
    <insert id="insertUser" parameterType="com.geoweb.demo.entity.User">
        INSERT INTO user (username, password, email)
        VALUES (#{username}, #{password}, #{email})
    </insert>
    <select id="findByUsername" parameterType="String" resultType="com.geoweb.demo.entity.User">
        SELECT id, username, password, email, create_time as createTime FROM user WHERE username = #{username}
    </select>
    <select id="findAll" resultType="com.geoweb.demo.entity.User">
        SELECT id, username, password, email, create_time as createTime FROM user
    </select>
</mapper> 