# apps

Purpose
- Deployable services and UI entrypoints.
- Each subfolder can run independently.

Contents
- backend: Spring Boot API gateway and user module.
- frontend: Vue 3 + Vite client.
- python-service: FastAPI compute service.

Notes
- Load only the relevant app folder for scoped changes.
