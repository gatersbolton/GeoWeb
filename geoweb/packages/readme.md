# packages

Purpose
- Reusable modules and compute packages.

Contents
- compute: algorithm modules.
- geo-core: shared domain logic (planned).
- shared-utils: shared schemas and constants (planned).

Notes
- Keep module boundaries clear for reuse.
