# env configs

Environment-based runtime configuration files.

For agent LLM setup, copy `agent.env.example` to a local env file and load it before starting the API.

Security note: do not commit real API keys.
